{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/music-rec-api/package.json",
                "localRoot": "${workspaceFolder}/music-rec-api"
            }
        }
    ]
}